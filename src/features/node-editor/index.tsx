import ReactDOM from 'react-dom';
import NodeEditorApp from './NodeEditorApp';

// Render the NodeEditorApp component into the 'root' element in the HTML document
ReactDOM.render(<NodeEditorApp />, document.getElementById('root'));
